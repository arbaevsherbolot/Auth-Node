CREATE TABLE postsDB (
    `id` INT AUTO_INCREMENT PRIMARY KEY,
    `img` VARCHAR(455),
    `title` VARCHAR(60),
    `short_desc` VARCHAR(300),
    `description` TEXT,
    `date` VARCHAR(30),
    `type` VARCHAR(20)
);